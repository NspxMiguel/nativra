# Cloud work log — 2026-09

Cloud session (no console access). Deliverables are code plus automated tests
that run on the GitHub Actions Windows runner. This file records what was built
and, for anything that can only be confirmed on the Xbox, exactly what to
measure there.

Two goals, worked in order:

1. **32-bit (x86) games** — a WoW64-style x86-to-x64 translation layer so PE32
   executables (WAVESHAPER, GTA San Andreas, Two Worlds II, Company of Heroes 2,
   many older titles) can start.
2. **Direct3D 9 on Direct3D 11** — `IDirect3D9`/`IDirect3DDevice9` over D3D11 so
   D3D9-only games and Adobe AIR's Stage3D can render.

---

## Goal 1 — x86 layer

### Done

**Portable core (`x86/Nativra.X86`, netstandard2.0).** Referenced by the UWP
app; unit-tested on net8.0 on the runner and locally.

- `Cpu/Decoder.cs` — instruction length + operand decoder for the one-byte,
  `0F`, `0F38`/`0F3A` and x87 maps, with VEX length skipping. Getting length
  exact is what lets the JIT step over instructions it does not translate.
- `Cpu/Interpreter*.cs` — a reference interpreter: integer ALU, shifts/rotates,
  mul/div/imul, BCD, string ops (`rep` forms), and the two-byte map
  (setcc/cmovcc, bit ops, movzx/movsx, shld/shrd, cmpxchg, xadd, bswap, …).
- `Cpu/Fpu.X87.cs`, `Cpu/Fpu.Sse.cs` — the x87 stack (80-bit registers kept in
  FXSAVE layout), MMX, and SSE/SSE2. CPUID advertises only what the interpreter
  actually executes (up to SSE2).
- `Cpu/GuestMemory.cs` — a 4 GB paged guest address space with two backings: a
  managed one for the interpreter and the tests (any OS), and a native 4 GB
  reservation where guest address + host base = host pointer, which the JIT
  addresses directly.

**Differential test against real silicon.** `x86/harness/oracle.c` is a 32-bit
program that, for each snippet in `snippets.txt`, builds random guest states,
runs the snippet natively (under WoW64 on the runner), and prints the state
before and after — registers, EFLAGS, the full FXSAVE image, and the touched
data and stack. `InterpreterOracleTests` replays each input through the
interpreter and checks it reaches the same state. **132 snippets pass
bit-for-bit**, covering every category above. The only masked fields are the
sticky FP exception-status bits (FSW/MXCSR), which no software interpreter
reproduces and no correct guest depends on; the JIT, running on real silicon,
reproduces them exactly.

**CI.** `.github/workflows/x86-harness.yml` (windows-latest) builds the oracle
with MSVC's x86 toolchain, regenerates the vectors on the runner's own
processor, and runs the suite. Independent of `build-uwp.yml`.

**x86-to-x64 block JIT (`x86/Nativra.X86/Jit`).** Translates a run of
straight-line guest code into one self-contained x64 function and caches it by
guest EIP.

- Guest GPRs are pinned to host GPRs (`Ctx.GuestToHost`), so a guest ALU
  instruction becomes the identical x64 instruction on the mapped registers and
  its EFLAGS come straight from the host processor — no flag recomputation, and
  a natural bit-for-bit match with real silicon.
- Guest memory is addressed as `host_base + guest_address` (the native 4 GB
  reservation), the standard box64/blink trick, so a load is one `lea` for the
  address and one `mov` through the base register.
- `CodeCache` publishes each block into its own page, committed RW, filled, then
  flipped to RX — the console's W^X route (RWX up front is refused; JitProbe
  measured this).
- The engine is JIT-first with an interpreter fallback: a block ends at the
  first control-flow or untranslated instruction and the reference interpreter
  runs that one, so the JIT is only ever faster, never a second source of truth
  that could diverge.
- Covered inline: the ALU set (reg/reg, reg/mem, mem/reg, immediates), mov in
  every form, lea, test, inc/dec/neg/not, shifts/rotates by imm/1/CL, mul/imul/
  div/idiv, imul 2- and 3-operand, movzx/movsx, setcc, cmovcc, bswap, push/pop.
- The **same hardware oracle** validates the JIT: every case runs through the
  compiled code and is compared to real silicon with **no flag mask at all**
  (the flags are the host CPU's own). 308 differential checks pass, plus
  end-to-end engine tests (a summation loop, an array walk through guest memory,
  a call/ret across the guest stack, block-cache reuse).

### In progress

- 32-bit PE mapping + relocation + TEB/PEB.

### To measure on the console (later)

- Nothing yet — no x86 game reaches the console path until the PE32 loader and
  import thunks are wired into `NativeProbe`. The JIT's own gate (executable
  pages via the RW→RX flip) is already proven on the console by JitProbe.

---

## Goal 2 — D3D9 on D3D11

Not started.

---

## Memory — Nativra's own share of the 5 GB

The console gives a UWP app 5120 MB, graphics memory included
(`MemoryManager.AppMemoryUsageLimit`), and Hades runs at about 4.4 GB. These
changes cut what the loader and the bridges hold for themselves. The platform
limit is not touched.

**Loader.** Before, a game module was held two or three times on top of its
mapped image. `NativeProbe` read it through `FileIO.ReadBufferAsync().ToArray()`,
which gives a WinRT buffer plus a managed copy on the large-object heap.
`LoaderStubs` read it through `FileWatch.ReadAll`, which gives a native
staging buffer plus the array. `ImageFile` now reads through the FromApp
handle (or a StorageFile stream when the handle is refused). Only the headers
come back as an array, and each section is read straight into the reserved
image. The build measures this: `uwp/Tools/MemoryBench` maps the runner's
largest DLLs with the app's own `PeImage` both ways, and `build-uwp` prints
the peak commit of each ("loader peak commit over baseline: old X MB, new Y
MB") and fails if the new way does not cost less.

**Frame mirror.** One CPU buffer instead of two (the in-flight flag already
kept the second one idle), which saves 8 MB at 1080p. The buffer is reused
across resizes, and a frame allocates nothing: no per-frame delegates, native
blocks, closures or streams. The staging ring stays at two textures in the
game's own format, because one would make the CPU wait for every frame. The
fake swap chain already made one back buffer and released the old one on
resize.

**Caches.** Four collections could grow for the whole session and are now
capped: FileWatch's attribute cache (8192 paths, then it starts over), the
`.xml` handle set (1024), and the two Steam callback queues (the newest 256).
The other lists were checked and are already bounded, or sized by what the
game imports.

### To measure on the console

Run **Hades** as usual (no marker needed) and play past the first room.
Then send back the `memory` line from `native-pulse.txt`, taken twice: once
on the title screen and once in the first room.

```
memory app=… MB limit=5120 MB commit=… MB peak-commit=… MB images=… MB mirror=… MB managed=… MB
```

- `app`: what the platform counts against the limit, GPU included.
- `commit`/`peak-commit`: the app's private commit now and at its peak.
  Loading is where the old whole-file copies showed up, so a lower peak is
  the loader change at work.
- `images`: bytes of game modules mapped by the loader.
- `mirror`: the frame mirror's staging textures, CPU buffer and picture.
- `managed`: the .NET heap.

What is left after subtracting `images`, `mirror` and `managed` from `app`
is the game's own allocations plus the graphics driver's, and that is where
the next cut has to come from.
